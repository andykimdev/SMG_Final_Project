data:
  string_file: "data/raw/protein_interactions.tsv"
  hpa_file: "data/raw/hpa_plasma_proteins.csv"
  min_score: 700

model:
  hidden_dim: 64
  latent_dim: 16
  dropout: 0.1

training:
  epochs: 100
  learning_rate: 0.01
  weight_decay: 0.0005
  device: "cuda"